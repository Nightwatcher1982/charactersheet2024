# 部署指南

本文档介绍如何将 D&D 2024 角色卡创建工具部署到各种平台，让你的应用可以在线访问。

## 📱 部署目标

部署后，你可以：
- 通过互联网访问应用
- 在手机上直接使用
- 分享给朋友使用
- 获得一个固定的网址

## 🚀 推荐部署平台

### 1. Vercel（最推荐）

**优点**：
- 免费
- 最适合 Next.js 应用
- 自动 HTTPS
- 全球 CDN 加速
- 一键部署

**步骤**：

1. 安装 Vercel CLI
```bash
npm install -g vercel
```

2. 在项目目录运行
```bash
vercel
```

3. 按照提示完成部署
   - 登录或注册 Vercel 账号
   - 选择项目设置
   - 等待部署完成

4. 获得部署网址
```
https://your-app.vercel.app
```

**通过 Git 部署**：

1. 将代码推送到 GitHub
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/your-username/dnd-character.git
git push -u origin main
```

2. 访问 [vercel.com](https://vercel.com)
3. 点击 "Import Project"
4. 连接 GitHub 仓库
5. 自动部署完成

### 2. Netlify

**优点**：
- 免费
- 简单易用
- 自动 HTTPS

**步骤**：

1. 安装 Netlify CLI
```bash
npm install -g netlify-cli
```

2. 构建项目
```bash
npm run build
```

3. 部署
```bash
netlify deploy --prod
```

**通过 Git 部署**：

1. 访问 [netlify.com](https://netlify.com)
2. 连接 GitHub 仓库
3. 设置构建命令：`npm run build`
4. 设置发布目录：`.next`
5. 点击 "Deploy"

### 3. Cloudflare Pages

**优点**：
- 免费
- 全球 CDN
- 速度快

**步骤**：

1. 访问 [pages.cloudflare.com](https://pages.cloudflare.com)
2. 连接 Git 仓库
3. 构建设置：
   - 构建命令：`npm run build`
   - 输出目录：`.next`
4. 部署

### 4. Railway

**优点**：
- 支持 Node.js 运行时
- 免费额度
- 简单配置

**步骤**：

1. 访问 [railway.app](https://railway.app)
2. 创建新项目
3. 连接 GitHub 仓库
4. 自动检测 Next.js
5. 部署

## 🔧 环境配置

### package.json 检查

确保有以下脚本：

```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  }
}
```

### next.config.js

已经配置好，无需修改。

### 静态导出（可选）

如果想生成纯静态站点：

1. 修改 `next.config.js`：
```javascript
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
}
```

2. 构建：
```bash
npm run build
```

3. 输出目录：`out/`

4. 可以部署到任何静态托管服务（GitHub Pages、Surge 等）

## 📱 移动端访问配置

### PWA 支持（可选）

如果想让应用可以"安装"到手机主屏幕：

1. 安装依赖
```bash
npm install next-pwa
```

2. 配置 `next.config.js`
```javascript
const withPWA = require('next-pwa')({
  dest: 'public',
  disable: process.env.NODE_ENV === 'development',
})

module.exports = withPWA({
  // ... 其他配置
})
```

3. 创建 `public/manifest.json`：
```json
{
  "name": "DND 2024 角色卡",
  "short_name": "DND 角色卡",
  "description": "D&D 2024 角色创建工具",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#ffffff",
  "theme_color": "#dc2626",
  "icons": [
    {
      "src": "/icon-192.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "/icon-512.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ]
}
```

## 🌐 自定义域名

### Vercel

1. 在 Vercel 项目设置中找到 "Domains"
2. 添加你的域名
3. 配置 DNS 记录
4. 等待 SSL 证书生成

### DNS 配置示例

添加以下 DNS 记录：

```
A     @     76.76.21.21
CNAME www   cname.vercel-dns.com
```

## 🔐 安全配置

### 环境变量

如果需要添加环境变量：

1. 在部署平台添加环境变量
2. 创建 `.env.local` 文件（不要提交到 Git）
```
NEXT_PUBLIC_ANALYTICS_ID=xxx
```

### CORS 配置

当前应用不需要 CORS 配置，所有数据都在前端处理。

## 📊 性能优化

### 已优化项目

✅ 代码分割（Next.js 自动）
✅ 图片优化（使用 Next.js Image）
✅ CSS 优化（Tailwind CSS）
✅ 树摇优化（自动）

### 额外优化建议

1. **启用压缩**（部署平台通常自动启用）
2. **CDN 加速**（Vercel/Netlify 自带）
3. **缓存策略**（Next.js 自动处理）

## 🐛 部署问题排查

### 构建失败

1. 检查 Node.js 版本（推荐 18+）
```bash
node --version
```

2. 清除缓存重新安装
```bash
rm -rf node_modules .next
npm install --cache ./.npm-cache --legacy-peer-deps
npm run build
```

### 页面空白

1. 检查浏览器控制台错误
2. 确认所有依赖都已安装
3. 检查 `app/layout.tsx` 和 `app/page.tsx`

### PDF 导出不工作

1. 确认 `jspdf` 和 `html2canvas` 已安装
2. 检查浏览器是否阻止下载
3. 某些移动浏览器可能不支持，建议使用 Chrome

## 📈 监控和分析

### Vercel Analytics

在 Vercel 项目设置中启用 Analytics，可以看到：
- 访问量
- 性能指标
- 用户地理位置

### Google Analytics（可选）

1. 在 `app/layout.tsx` 添加 GA 代码
2. 配置环境变量

## 💰 成本估算

### 免费方案对比

| 平台 | 带宽 | 构建时间 | 并发用户 |
|------|------|----------|----------|
| Vercel | 100GB/月 | 6000分钟/月 | 无限制 |
| Netlify | 100GB/月 | 300分钟/月 | 无限制 |
| Cloudflare | 无限 | 500次/月 | 无限制 |

### 升级建议

对于个人或小团队使用，免费方案完全够用。
超出免费额度后可以考虑升级到付费方案。

## 🎯 部署检查清单

部署前检查：

- [ ] 所有依赖已安装
- [ ] `npm run build` 成功
- [ ] 本地测试通过
- [ ] 已添加 `.gitignore`
- [ ] README.md 已更新

部署后检查：

- [ ] 网站可以访问
- [ ] 创建角色功能正常
- [ ] PDF 导出正常
- [ ] 移动端显示正常
- [ ] 数据保存功能正常

## 📚 参考资源

- [Next.js 部署文档](https://nextjs.org/docs/deployment)
- [Vercel 文档](https://vercel.com/docs)
- [Netlify 文档](https://docs.netlify.com)
- [Cloudflare Pages 文档](https://developers.cloudflare.com/pages)

## 🆘 获取帮助

如果遇到部署问题：

1. 查看部署平台的日志
2. 检查构建输出
3. 搜索错误信息
4. 查看平台文档

---

**祝部署顺利！🚀**
