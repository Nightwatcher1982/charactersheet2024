# 必须与角色卡项目 .env 中的 JWT_SECRET 完全一致（至少 32 字符），否则登录后仍会显示「暂无角色」
JWT_SECRET="your_jwt_secret_key_at_least_32_characters_long"

# 角色卡服务 base URL（服务端请求用）。本地开发填 http://localhost:3000；生产同机部署填 http://127.0.0.1:3100
CHARACTER_SHEET_API_URL="http://localhost:3000"

# 战役库（必须与角色卡库分离：同实例不同库，如 postgresql://dnd_user:密码@localhost:5432/dm_campaigns）
# 服务器部署前需在 PostgreSQL 中创建 dm_campaigns 并执行 npx prisma migrate deploy，见 docs/战役大厅部署说明.md
DATABASE_URL="postgresql://user:password@localhost:5432/dm_campaigns"

# 角色卡登录页 URL（前端「去登录」、回调跳转用）。本地 http://localhost:3000，生产 https://cs.dimvision.xyz
NEXT_PUBLIC_CHARACTER_SHEET_URL="http://localhost:3000"

# 战役背景图上传到 OSS（与主项目角色卡共用同一 Bucket 与密钥即可）
# NEXT_PUBLIC_PIC_BASE_URL="https://你的Bucket.oss-cn-shanghai.aliyuncs.com/charactersheet2024"
# OSS_BUCKET="你的Bucket名"
# OSS_REGION="oss-cn-shanghai"
# ALIYUN_ACCESS_KEY_ID=""
# ALIYUN_ACCESS_KEY_SECRET=""
