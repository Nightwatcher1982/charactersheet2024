# 🐛 修复：物种技能被错误计入职业技能数量

**问题报告**：吟游诗人+木精灵，选择"生存"作为物种技能后，职业技能选择时"生存"被默认选上，导致只能再选2个技能而不是3个。

**完成时间**：刚才  
**状态**：✅ 已修复

---

## 🔍 问题分析

### 用户场景
```
1. 选择职业：吟游诗人（应选3个职业技能）
2. 选择背景：流浪者（获得：洞悉、隐匿）
3. 选择物种：精灵 - 木精灵（选择：生存）
4. 步骤5：选择职业技能
   ❌ 问题："生存"被默认选上
   ❌ 只能再选2个技能（杂技、奥秘）
   ❌ 应该可以选3个全新的技能
```

### 问题原因

#### 之前的逻辑（错误）
```typescript
// 只排除了背景技能，没有排除物种技能
const classSkills = skills.filter(skill => 
  classData?.availableSkills?.includes(skill) && 
  !backgroundData?.skills?.includes(skill)
);

// 当前技能：['洞悉', '隐匿', '生存']
// 吟游诗人可用技能包括：生存
// classSkills = ['生存']（1个）

// needsClassSkills = 1 < 3 = true
// 提示："还需选择 2 项技能"
// ❌ 错误！应该是"还需选择 3 项技能"
```

#### 核心问题
**物种技能没有被排除**：
- 背景技能（洞悉、隐匿）被正确排除 ✅
- 物种技能（生存）没有被排除 ❌

如果物种技能恰好在职业可用技能列表中（如吟游诗人的"生存"），它会被错误地计入职业技能数量。

---

## ✅ 修复方案

### 修改文件：`components/steps/StepSkills.tsx`

#### 修复1：提取物种技能

```typescript
// 获取物种技能
let speciesSkills: string[] = [];
if (currentCharacter?.classFeatureChoices?.speciesChoices) {
  try {
    const speciesChoices = JSON.parse(
      currentCharacter.classFeatureChoices.speciesChoices as string
    );
    if (speciesChoices.skill) {
      // 从"生存（感知）- 追踪、狩猎..."中提取"生存"
      const skillMatch = speciesChoices.skill.match(/^([^（]+)/);
      if (skillMatch) {
        speciesSkills = [skillMatch[1]];
      }
    }
  } catch (e) {
    // 解析失败
  }
}
```

#### 修复2：排除物种技能

```typescript
// 分类技能
const backgroundSkills = skills.filter(skill => 
  backgroundData?.skills?.includes(skill)
);

// 职业技能：在职业可用技能中，但不是背景技能，也不是物种技能
const classSkills = skills.filter(skill => 
  classData?.availableSkills?.includes(skill) && 
  !backgroundData?.skills?.includes(skill) &&
  !speciesSkills.includes(skill)  // ✅ 新增：排除物种技能
);
```

#### 修复3：保留物种技能

```typescript
// 处理职业技能选择完成
const handleClassSkillsComplete = (selectedSkills: string[]) => {
  const currentSkills = currentCharacter.skills || [];
  
  // 保留背景技能、物种技能和其他非职业技能
  const nonClassSkills = currentSkills.filter(skill => 
    backgroundData?.skills?.includes(skill) ||
    speciesSkills.includes(skill) ||  // ✅ 新增：保留物种技能
    !classData?.availableSkills?.includes(skill)
  );
  
  // 添加新选择的职业技能
  updateCurrentCharacter({
    skills: [...nonClassSkills, ...selectedSkills]
  });
};
```

---

## 🎯 修复效果

### 修复后的逻辑

#### 场景：吟游诗人 + 流浪者 + 木精灵

```
1. 初始状态（完成物种选择后）：
   skills = ['洞悉', '隐匿', '生存']
   backgroundSkills = ['洞悉', '隐匿']（2个）
   speciesSkills = ['生存']（1个）← 新提取
   classSkills = []（0个，排除了背景和物种技能）
   needsClassSkills = 0 < 3 = true ✅
   提示："还需选择 3 项技能"✅

2. 用户选择"杂技"：
   skills = ['洞悉', '隐匿', '生存', '杂技']
   classSkills = ['杂技']（1个）
   needsClassSkills = 1 < 3 = true ✅
   提示："还需选择 2 项技能"✅

3. 用户选择"奥秘"：
   skills = ['洞悉', '隐匿', '生存', '杂技', '奥秘']
   classSkills = ['杂技', '奥秘']（2个）
   needsClassSkills = 2 < 3 = true ✅
   提示："还需选择 1 项技能"✅

4. 用户选择"表演"：
   skills = ['洞悉', '隐匿', '生存', '杂技', '奥秘', '表演']
   classSkills = ['杂技', '奥秘', '表演']（3个）
   needsClassSkills = 3 < 3 = false ✅
   "✓ 技能选择完成！"✅

最终技能：6项
- 洞悉（背景）
- 隐匿（背景）
- 生存（物种）
- 杂技（职业）
- 奥秘（职业）
- 表演（职业）
```

---

## 🧪 测试场景

### 测试场景1：吟游诗人 + 木精灵（用户报告）

```
⚠️ 强制刷新：Cmd+Shift+R (Mac) 或 Ctrl+Shift+R (Windows)

1. http://localhost:3000

2. 创建角色：
   → 吟游诗人 + 流浪者背景

3. 步骤2.2：选择精灵
   → 选择"木精灵（速度35尺...）"
   → 选择"生存（感知）- 追踪、狩猎..."
   → 完成

4. 步骤5：选择职业技能
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   【关键检查点】
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   
   ✅ 背景技能已获得：
      • 洞悉
      • 隐匿
   
   ✅ 提示："从以下技能中选择 3 项技能熟练"
      "还需选择 3 项技能"← 应该是3，不是2
   
   ✅ "生存"不应该被默认选上
   ✅ "生存"在技能列表中是可选的（但未选中）
   
5. 选择职业技能：
   ✅ 选择"杂技"→"还需选择 2 项"
   ✅ 选择"奥秘"→"还需选择 1 项"
   ✅ 选择"表演"→"✓ 技能选择完成！"
   
6. 步骤6：技能纵览
   ✅ 应该显示 6 项熟练：
      • 洞悉（背景）
      • 隐匿（背景）
      • 生存（物种）
      • 杂技（职业）
      • 奥秘（职业）
      • 表演（职业）
```

### 测试场景2：人类游侠（另一个可能场景）

```
职业：游侠（skillChoices: 3）
  → availableSkills: 包括"生存"

物种：人类
  → 选择"竞技"作为人类技能

背景：向导
  → skills: ['自然', '生存']

步骤5：选择职业技能
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  背景技能：自然、生存
  物种技能：竞技
  
  ✅ 提示："还需选择 3 项技能"
  ✅ "自然"和"生存"不会被默认选上
  ✅ "竞技"不会被默认选上
  
  选择3个全新的技能：
  → 如：运动、察觉、隐匿
  
  最终：6项技能
  • 自然（背景）
  • 生存（背景）
  • 竞技（物种）
  • 运动（职业）
  • 察觉（职业）
  • 隐匿（职业）
```

### 测试场景3：精灵选择"洞悉"（与背景重复）

```
职业：牧师（skillChoices: 2）
背景：侍僧（skills: ['洞悉', '宗教']）
物种：精灵
  → 选择"洞悉"作为物种技能

问题：洞悉既是背景技能，又被选为物种技能

步骤5：选择职业技能
  ✅ 背景技能：洞悉、宗教
  ✅ 物种技能：洞悉（重复）
  ✅ 提示："还需选择 2 项技能"
  ✅ 不会因为重复而少选
  
最终：4项技能（去重）
  • 洞悉（背景/物种）
  • 宗教（背景）
  • 医药（职业，假设）
  • 说服（职业，假设）
```

---

## 📊 对比

### 修复前（❌ 错误）

```
吟游诗人 + 流浪者 + 木精灵（选择生存）

步骤5：
  当前技能：洞悉、隐匿、生存
  计算：
    backgroundSkills = [洞悉, 隐匿]
    classSkills = [生存]  ❌ 错误计入
  
  提示："还需选择 2 项技能"❌
  
  实际情况：
  • 用户只能选2个新技能
  • 最终只有5项技能
  ❌ 少了1个职业技能
```

### 修复后（✅ 正确）

```
吟游诗人 + 流浪者 + 木精灵（选择生存）

步骤5：
  当前技能：洞悉、隐匿、生存
  计算：
    backgroundSkills = [洞悉, 隐匿]
    speciesSkills = [生存]  ✅ 正确提取
    classSkills = []  ✅ 排除了物种技能
  
  提示："还需选择 3 项技能"✅
  
  实际情况：
  • 用户可以选3个新技能
  • 最终有6项技能
  ✅ 完整的职业技能数量
```

---

## 🔄 影响范围

### 受影响的组合

任何**物种技能在职业可用技能中**的组合都会受影响：

#### 吟游诗人（18个可用技能）
- ✅ 精灵选择：洞悉、察觉、生存 ← 都在吟游诗人列表中
- ✅ 人类选择：任意1个技能

#### 游侠（8个可用技能）
- ✅ 精灵选择：察觉、生存 ← 都在游侠列表中

#### 德鲁伊（8个可用技能）
- ✅ 精灵选择：察觉、生存 ← 都在德鲁伊列表中

#### 牧师（5个可用技能）
- ✅ 精灵选择：洞悉 ← 在牧师列表中

#### 其他职业
- 如果物种技能不在职业列表中，不受影响

---

## ✅ 修复完成

### 修改总结

| 问题 | 状态 | 修改位置 |
|------|------|----------|
| 物种技能被计入职业技能数量 | ✅ 已修复 | StepSkills.tsx:47-62 |
| 物种技能未被保留 | ✅ 已修复 | StepSkills.tsx:64-74 |

### 技术实现

#### 1. 提取物种技能
- 解析 `classFeatureChoices.speciesChoices`
- 从选项文本中提取技能名称
- 存储在 `speciesSkills` 数组中

#### 2. 排除物种技能
- 在计算 `classSkills` 时
- 同时排除背景技能和物种技能
- 确保计数准确

#### 3. 保留物种技能
- 在更新职业技能时
- 保留背景技能、物种技能
- 只替换职业技能

---

## 📝 完整的技能分类逻辑

### 技能来源
```
角色技能 = 背景技能 + 物种技能 + 职业技能

示例：吟游诗人 + 流浪者 + 木精灵
- 背景技能（2）：洞悉、隐匿
- 物种技能（1）：生存
- 职业技能（3）：杂技、奥秘、表演
- 总计（6）项技能熟练
```

### 计算规则
```typescript
// 1. 背景技能：固定，来自背景数据
backgroundSkills = skills.filter(skill => 
  backgroundData?.skills?.includes(skill)
);

// 2. 物种技能：来自物种选择
speciesSkills = parseSpeciesChoices(
  currentCharacter.classFeatureChoices.speciesChoices
);

// 3. 职业技能：可用技能中，排除背景和物种
classSkills = skills.filter(skill => 
  classData?.availableSkills?.includes(skill) &&
  !backgroundData?.skills?.includes(skill) &&
  !speciesSkills.includes(skill)
);

// 4. 检查是否需要选择更多职业技能
needsClassSkills = classSkills.length < classData.skillChoices;
```

---

## ✅ 验证清单

### 功能验证
- [ ] 物种技能不被计入职业技能数量
- [ ] 职业技能提示数量正确
- [ ] 可以选择完整数量的职业技能
- [ ] 物种技能在最终技能列表中显示
- [ ] 物种技能有熟练标记

### 边界情况
- [ ] 物种技能与背景技能重复（如精灵选洞悉，背景也有洞悉）
- [ ] 物种技能不在职业可用列表中
- [ ] 没有物种技能选择的物种（如矮人无技能选择）

---

## 🎯 现在可以测试了！

**状态**：✅ 修复完成  
**编译**：✅ 成功  
**测试**：✅ 立即可用

### 快速验证（2分钟）
```
1. 强制刷新：Cmd+Shift+R

2. 创建：吟游诗人 + 流浪者 + 木精灵
   → 选择"生存"作为物种技能

3. 步骤5：
   ✅ 应该提示"还需选择 3 项技能"
   ✅ "生存"不应该被默认选上
   ✅ 可以选择3个全新的职业技能

4. 最终：6项技能熟练
```

**现在吟游诗人可以正确选择3个职业技能了！** 🎉✨
